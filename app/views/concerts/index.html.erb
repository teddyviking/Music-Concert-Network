<h1>Next concerts</h1>


<ul>
	<% @concerts.each do |concert| %>
		<li><%= link_to (concert.band + " at " + concert.venue), concert_path(concert) %> </br>
			on <%= concert.date %>
		 </li>

	<% end %>

</ul>